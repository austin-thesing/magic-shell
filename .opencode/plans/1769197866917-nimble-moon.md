# Plan: Bump Version, Commit, Push

## Goal
Update the package version, remove MiniMax M2.1 from Zen, commit the recent UI/model fixes, and push to the remote.

## Scope
- `package.json`
- `src/cli.ts`
- `src/lib/types.ts`

## Plan
1. Remove the MiniMax M2.1 entry from `OPENCODE_ZEN_MODELS` in `src/lib/types.ts`.
2. Update `package.json` version to `0.2.11`.
3. Review `git status` and `git diff` to confirm staged/untracked files are relevant.
4. Stage changes: `package.json`, `src/cli.ts`, `src/lib/types.ts`.
5. Commit with conventional commit style and include version bump.
6. Push to the current branch.

## Verification
- Run `bun run typecheck` if desired.
- Run `bun run build` if desired.
- Launch TUI (`bun run dev:tui`) and verify text selection works.
