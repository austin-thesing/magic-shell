#!/usr/bin/env bun

/**
 * Magic Shell TUI - Direct entry point for interactive mode
 * 
 * This is a simple wrapper that launches the TUI directly.
 * For CLI mode, use `msh` instead.
 */

import "./cli"
